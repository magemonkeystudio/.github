name: Reusable Maven Build

on:
  workflow_call:
    inputs:
      maven_args:
        description: 'Extra arguments for the Maven package command (e.g. -f pom-dev.xml)'
        type: string
        default: ''
      java_version:
        description: 'Java version(s) to install. Use a multiline string for multiple versions.'
        type: string
        default: '21'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: ${{ inputs.java_version }}
          server-id: ossrh
          settings-path: ${{ github.workspace }}
          gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }}
          gpg-passphrase: MAVEN_GPG_PASSPHRASE
      - name: Create settings.xml
        uses: s4u/maven-settings-action@v3.0.0
        with:
          githubServer: false
      - name: Build with Maven
        run: mvn clean package -e ${{ inputs.maven_args }}
